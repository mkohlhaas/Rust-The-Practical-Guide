#![allow(unused_macros)]

// Declarative Macros - Basic Syntax

macro_rules! macro_name {
// Match rules
// matching pattern => code substitution
    (...) => { ... };
    (...) => { ... };
    (...) => { ... };
}

fn main() {}
