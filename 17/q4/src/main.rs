use std::path::{Path, PathBuf};
fn main() {
    let mut path = PathBuf::new();
    let components = vec!["C:", "Users", "Public", "example.txt"];
    // Task 1: Add code to dynamically build the path using the components vector.
    // Task 2: if the path exists as a file then display its metadata. Otherwise display that the path does not exists as a file   
}