fn products_in_range(root: Node, low: i32, high: i32) -> Vec<i32> {
  let mut output: Vec<i32> = Vec::new();
  // recursively checks all the nodes for the three conditions outlined in Section 1.7.2
  output
}
